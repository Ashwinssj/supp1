    # Environment variables
    .env

    # Python cache
    __pycache__/
    *.pyc
    *.pyo
    *.pyd

    # Virtual environment
    venv/
    env/
    .venv/

    # IDE / OS specific
    .idea/
    .vscode/
    *.DS_Store

    # Uploads directory (if you don't want uploaded data in Git)
    # Note: Render's filesystem is ephemeral, see Step 5 below.
    uploads/
