streamlit
pandas
python-dotenv # Good practice even if Render uses env vars differently
google-generativeai
plotly
openpyxl # Needed by pandas for Excel export/read if not already installed
xlsxwriter # Explicitly used for Excel export
# Add any other libraries used by data_processor.py
